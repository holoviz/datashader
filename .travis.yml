# TODO before merge:
#  * what channels?
#  * restore previous sections
#  * restore guards for various sections

language: generic

sudo: false

os:
  - linux

env:
  global:
    - PYENV_VERSION=3.6
    - DOIT="doit --db-file=$HOME/extracache/.doitdb"
    - CHANNELS="-c pyviz/label/dev -c conda-forge"
    # conda build fills up travis /tmp (tmpfs)
    - TMPDIR=$HOME/tmp

cache:
  directories:
    - $HOME/miniconda
    - $HOME/extracache

before_cache:
  - rm -rf $HOME/miniconda/pkgs
  - rm -rf $HOME/miniconda/conda-bld/*

# quick hack to determine what tag is (improvements welcomed)
#     release: ^v(\d+|\.)*[^a-z]\d*$
# dev release: ^v(\d+|\.)*[a-z]\d*$

stages:
  - test
  - name: conda_dev_package
    if: tag =~ ^v(\d+|\.)*[a-z]\d*$
  - name: pip_dev_package
    if: tag =~ ^v(\d+|\.)*[a-z]\d*$
#  - name: conda_package
#    if: tag =~ ^v(\d+|\.)*[^a-z]\d*$
#  - name: pip_package
#    if: tag =~ ^v(\d+|\.)*[^a-z]\d*$

jobs:
  include:

    ########## TEST DEVELOPER INSTALL ##########

    # quick tests in typical dev env (most tests are run on packages)

    - &conda_default
      stage: test
      env: DESC="dev test_all_quick"
      before_install:
        # brew-installed geos interferes with conda's cartopy?
        - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then
            brew uninstall --ignore-dependencies geos gdal postgis;
          fi
        # install doit/pyct and use to install miniconda...
        - pip install pyct && doit miniconda_install && pip uninstall -y doit pyct
        - export PATH="$HOME/miniconda/bin:$PATH" && hash -r
        - conda config --set always_yes True
        # ...and now install doit/pyct into miniconda
        - conda install -c pyviz/label/dev pyct && $DOIT ecosystem_setup
      install:
        - $DOIT env_create --python=$PYENV_VERSION
        - source activate test-environment
        # ...and now install doit/pyct into env!
        - conda install -c pyviz/label/dev pyct
        - $DOIT develop_install $CHANNELS
        - $DOIT env_capture
      script:
        - $DOIT test_all_quick

    # python 2 flake checking typically catches python 2 syntax
    # errors where python 3's been assumed...
    - <<: *conda_default
      env: DESC="py2 flakes" PYENV_VERSION=2.7
      script: $DOIT test_flakes


    ########## TEST END-USER PACKAGES ##########

    ## pip

    # TODO: need CONDADOIT and PIPDOIT to prevent cache confusion?

    - &python_default
      env: PYENV_VERSION=3.6 PYPI="https://test.pypi.org/legacy/"
      stage: pip_dev_package
      before_install: pip install pyct && doit ecosystem=pip ecosystem_setup
      install:
        - unset PYENV_VERSION && pyenv global 3.6 2.7
        - doit ecosystem=pip package_build --test-group=unit --testpython=py27 --testpython=py36
      script: doit ecosystem=pip package_upload -u $TPPU -p $TPPP -r ${PYPI}
      # Future work; see https://github.com/bokeh/datashader/issues/588
      #  doit ecosystem=pip package_build --test-group=examples_quick

    ## conda

    - <<: *conda_default
      stage: conda_dev_package
      install:
        - $DOIT package_build --recipe base $CHANNELS --test-python=py36 --test-python=py35 --test-python=py27 --test-group=unit
        - conda build purge # TODO: need to add flag to pyct
        - $DOIT package_build --recipe examples $CHANNELS --test-python=py36 --test-python=py35 --test-python=py27 --test-group=all_quick
      script: true
        - $DOIT package_upload --token=$CONDA_UPLOAD_TOKEN --recipe=base --label=dev
        - $DOIT package_upload --token=$CONDA_UPLOAD_TOKEN --recipe=examples --label=dev

notifications:
  email: false
